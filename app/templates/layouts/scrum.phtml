<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><?= $this->e($title ?? 'Tragwerk') ?></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <script src="/js/htmx.min.js"></script>
    <link rel="stylesheet" href="/css/pixel.css">
    <?= $this->section('head') ?>
</head>
<body>
<div class="scrum-app">

    <!-- ===== SIDEBAR ===== -->
    <aside class="sidebar">

        <!-- Logo -->
        <div class="sidebar-logo">
            <?= $this->fetch('partial::logo') ?>
        </div>

        <!-- Project selector -->
        <div class="project-selector">
            <span class="selector-label">Project</span>
            <div class="selector-value">PHOENIX</div>
        </div>

        <!-- Navigation -->
        <nav class="sidebar-nav">
            <div class="nav-section-label">Workspace</div>
            <a href="<?= $this->url('demo.board') ?>"
               class="nav-item <?= $this->e($activeNav ?? '') === 'board' ? 'active' : '' ?>">
                <span class="nav-icon">▦</span>
                Board
                <span class="nav-badge">7</span>
            </a>
            <a href="<?= $this->url('demo.backlog') ?>"
               class="nav-item <?= $this->e($activeNav ?? '') === 'backlog' ? 'active' : '' ?>">
                <span class="nav-icon">≡</span>
                Backlog
                <span class="nav-badge">34</span>
            </a>
            <a href="#" class="nav-item">
                <span class="nav-icon">⚡</span>
                Sprints
            </a>
            <div class="nav-section-label" style="margin-top:8px">Team</div>
            <a href="#" class="nav-item">
                <span class="nav-icon">♟</span>
                Members
                <span class="nav-badge">4</span>
            </a>
            <a href="#" class="nav-item">
                <span class="nav-icon">◈</span>
                Reports
            </a>
        </nav>

        <!-- Sprint status widget -->
        <div class="sprint-widget">
            <div class="sprint-widget-title">Current Quest</div>
            <div class="sprint-name-label">SPRINT 3</div>
            <div class="sprint-quest-name">"Into the Dark Forest"</div>
            <div class="sprint-day-label">
                <span>DAY 8 / 14</span>
                <span>57%</span>
            </div>
            <div class="hp-bar-wrap">
                <div class="hp-bar-fill hp-high" style="width: 57%"></div>
            </div>
            <div class="sprint-stats-row">
                <span>23 XP done</span>
                <span>40 XP total</span>
            </div>
            <div class="velocity-row">
                <span class="velocity-label">VEL</span>
                <?php
                $vel = 8; $maxVel = 10;
                for ($i = 0; $i < $maxVel; $i++):
                ?>
                <div class="vel-block <?= $i >= $vel ? 'empty' : '' ?>"></div>
                <?php endfor; ?>
            </div>
        </div>

        <!-- Sidebar footer / user -->
        <div class="sidebar-footer">
            <div class="user-row">
                <div class="pixel-avatar blue">BH</div>
                <span class="user-name">Benjamin H.</span>
            </div>
        </div>

    </aside>

    <!-- ===== MAIN CONTENT ===== -->
    <main class="main-content">
        <?= $this->section('content') ?>
    </main>

</div>
<?= $this->section('javascript') ?>
</body>
</html>
