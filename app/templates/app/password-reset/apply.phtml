<?php $this->layout('layout::auth') ?>

<div class="auth-card">
    <div class="auth-card__logo">
        <?= $this->fetch('partial::logo') ?>
    </div>
    <div id="reset-request">
        <?= $this->fetch('partial::link', [
                'href' => $this->url('login'),
                'label' => $this->t('Back to sign in'),
                'icon' => 'bi bi-arrow-left',
                'classes' => 'auth-back'
        ]) ?>
        <div class="auth-head">
            <h1><?= $this->t('Set new password') ?></h1>
            <p><?= $this->t('Enter and confirm your new password.') ?></p>
        </div>

        <form class="auth-form" action="<?= $this->url('passwordReset.apply', ['token' => $token]) ?>" method="post">
            <?= $this->fetch('partial::input/csrf') ?>
            <?= $this->fetch('partial::input/password', [
                    'id'         => 'password1',
                    'label'      => $this->t('New password'),
                    'required'   => true,
                    'autofocus'  => true,
                    'validation' => $validationBag ?? null,
            ]) ?>

            <?= $this->fetch('partial::input/password', [
                    'id'         => 'password2',
                    'label'      => $this->t('Confirm new password'),
                    'required'   => true,
                    'validation' => $validationBag ?? null,
            ]) ?>

            <?= $this->fetch('partial::input/button', [
                    'label'   => $this->t('Update password'),
                    'classes' => 'btn btn-primary btn-block',
            ]) ?>
        </form>
    </div>
</div>


