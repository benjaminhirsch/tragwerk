<?php $this->layout('layout::blank', ['title' => 'Deploy apps without the ops']) ?>

<header class="doc-bar">
    <div class="doc-bar__row lp-nav">
        <?= $this->fetch('partial::logo') ?>
        <nav class="lp-nav__links ms-4">
            <a href="#features">Features</a>
            <a href="#how">How it works</a>
            <a href="#">Pricing</a>
            <a href="https://docs.tragwerk.app/">Docs</a>
        </nav>
        <div class="ms-auto d-flex align-items-center gap-2">
            <a href="<?= $this->url('login') ?>" class="btn btn-ghost btn-sm">Sign in</a>
            <a href="<?= $this->url('register') ?>" class="btn btn-primary btn-sm">Get started</a>
            <button class="tw-iconbtn" id="auth-theme-btn" title="Switch theme"><i id="auth-theme-ico" class="bi bi-moon-stars"></i></button>
        </div>
    </div>
</header>

<div class="lp-wrap">

    <!-- HERO -->
    <section class="lp-hero">
        <div class="lp-hero__inner">
            <span class="lp-pill">
                <span class="tw-badge tw-badge--ok"><span class="tw-dot"></span>New</span>
                Bring Your Own Cloud · PaaS meets SaaS
            </span>
            <h1>Deploy without friction.<br><span class="grad">From commit to production in minutes.</span></h1>
            <p class="sub">The PaaS experience with a SaaS platform — on your own infrastructure. <strong>Bring Your Own Cloud:</strong> isolated environments per branch with instant data cloning for child branches, running on any Linux server from the hoster of your choice. No vendor lock-in.</p>
            <div class="lp-cta-row">
                <a href="<?= $this->url('register') ?>" class="btn btn-primary"><i class="bi bi-rocket-takeoff"></i> Start free</a>
                <a href="#" class="btn btn-outline-secondary">View pricing</a>
                <a href="#docs" class="btn btn-ghost">Read the docs <i class="bi bi-arrow-right"></i></a>
            </div>

            <!-- terminal mock -->
            <div class="lp-termbar">
                <div class="lp-code">
                    <div class="lp-code__bar"><i style="background:#ff5f57"></i><i style="background:#febc2e"></i><i style="background:#28c840"></i><span class="mono" style="font-size:.76rem;color:var(--text-faint);margin-left:.4rem">bash</span></div>
<pre><span class="c-dim">$</span> git push tragwerk main
<span class="c-dim">→</span> Detected <span class="c-ac">php:8.5</span> (FrankenPHP) · building image…
<span class="c-ok">✓</span> Build succeeded in 1m 48s
<span class="c-dim">→</span> Deploying 3 replicas to <span class="c-ac">eu-3</span>
<span class="c-ok">✓</span> Live at https://storefront.tragwerk.app <span class="c-dim">(42s)</span></pre>
                </div>
            </div>
        </div>
    </section>

    <!-- STATS -->
    <section class="lp-section" style="padding-top:1rem">
        <div class="lp-stats">
            <div class="lp-stat"><div class="lp-stat__num">&lt;60s</div><div class="lp-stat__label">Average deploy time</div></div>
            <div class="lp-stat"><div class="lp-stat__num">99.98%</div><div class="lp-stat__label">Platform uptime</div></div>
            <div class="lp-stat"><div class="lp-stat__num">6</div><div class="lp-stat__label">Global regions</div></div>
            <div class="lp-stat"><div class="lp-stat__num">0</div><div class="lp-stat__label">Servers to manage</div></div>
        </div>
    </section>

    <!-- FEATURES -->
    <section class="lp-section" id="features" style="padding-top:1rem">
        <div class="lp-section__head">
            <h2>Everything you need to run in production</h2>
            <p>From the first push to scaling under load — the platform handles the plumbing so your team ships features.</p>
        </div>
        <div class="lp-features">
            <div class="lp-feat">
                <div class="lp-feat__ico"><i class="bi bi-git"></i></div>
                <h3>Git-driven deploys</h3>
                <p>Push to a branch and Tragwerk builds, tests and rolls out automatically. Every pull request gets its own preview environment.</p>
            </div>
            <div class="lp-feat">
                <div class="lp-feat__ico"><i class="bi bi-diagram-3"></i></div>
                <h3>Isolated environments</h3>
                <p>Production, staging and per-branch previews, each with their own variables, domains and data — promoted with a click.</p>
            </div>
            <div class="lp-feat">
                <div class="lp-feat__ico"><i class="bi bi-graph-up"></i></div>
                <h3>Live metrics &amp; logs</h3>
                <p>Request rates, latency percentiles, worker utilization and streaming build/deploy logs — straight from the FrankenPHP runtime.</p>
            </div>
            <div class="lp-feat">
                <div class="lp-feat__ico"><i class="bi bi-hdd-stack"></i></div>
                <h3>Servers &amp; registries</h3>
                <p>Provision application, database and cache servers, and push private container images — all scoped to your team.</p>
            </div>
            <div class="lp-feat">
                <div class="lp-feat__ico"><i class="bi bi-people"></i></div>
                <h3>Teams &amp; roles</h3>
                <p>Organize projects under teams, invite members with granular roles, and enforce SSO and two-factor across the board.</p>
            </div>
            <div class="lp-feat">
                <div class="lp-feat__ico"><i class="bi bi-shield-lock"></i></div>
                <h3>Secure by default</h3>
                <p>Automatic TLS, encrypted secrets, isolated builds and audit logs — production-grade security without the setup.</p>
            </div>
        </div>
    </section>

    <!-- SPLIT: config as code -->
    <section class="lp-section" style="padding-top:1rem">
        <div class="lp-split">
            <div>
                <p class="tw-eyebrow mb-2">Configuration as code</p>
                <h2 style="font-size:clamp(1.6rem,3.5vw,2rem);letter-spacing:-0.02em;margin:0 0 .6rem">One file describes your whole environment</h2>
                <p style="color:var(--text-muted);font-size:1rem;line-height:1.6;margin:0">Applications, services, mounts, hooks, cron jobs and routes live in <span class="mono">tragwerk.xml</span> — a schema-validated file, versioned with your code and applied on every deploy.</p>
                <ul>
                    <li><i class="bi bi-check-circle-fill"></i> PostgreSQL, MariaDB, Redis, Valkey and more as managed services</li>
                    <li><i class="bi bi-check-circle-fill"></i> Build, deploy and post-deploy hooks</li>
                    <li><i class="bi bi-check-circle-fill"></i> Routes mapped to domain placeholders like <span class="mono">{default}</span></li>
                </ul>
            </div>
            <div class="lp-code">
                <div class="lp-code__bar"><i style="background:#ff5f57"></i><i style="background:#febc2e"></i><i style="background:#28c840"></i><span class="mono" style="font-size:.76rem;color:var(--text-faint);margin-left:.4rem">tragwerk.xml</span></div>
<pre><span class="c-dim">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span>
<span class="c-ac">&lt;project&gt;</span>
  <span class="c-ac">&lt;applications&gt;</span>
    <span class="c-ac">&lt;application</span> name=<span class="c-ok">"storefront"</span> type=<span class="c-ok">"php:8.5"</span><span class="c-ac">&gt;</span>
      <span class="c-ac">&lt;web&gt;</span>
        <span class="c-ac">&lt;location</span> path=<span class="c-ok">"/"</span> root=<span class="c-ok">"public"</span> passthru=<span class="c-ok">"/index.php"</span><span class="c-ac">/&gt;</span>
      <span class="c-ac">&lt;/web&gt;</span>
      <span class="c-ac">&lt;relationships&gt;</span>
        <span class="c-ac">&lt;relationship</span> name=<span class="c-ok">"database"</span> target=<span class="c-ok">"db"</span><span class="c-ac">/&gt;</span>
      <span class="c-ac">&lt;/relationships&gt;</span>
      <span class="c-ac">&lt;hooks&gt;</span>
        <span class="c-ac">&lt;hook</span> type=<span class="c-ok">"build"</span><span class="c-ac">&gt;</span>composer install --no-dev<span class="c-ac">&lt;/hook&gt;</span>
        <span class="c-ac">&lt;hook</span> type=<span class="c-ok">"deploy"</span><span class="c-ac">&gt;</span>bin/console doctrine:migrate<span class="c-ac">&lt;/hook&gt;</span>
      <span class="c-ac">&lt;/hooks&gt;</span>
    <span class="c-ac">&lt;/application&gt;</span>
  <span class="c-ac">&lt;/applications&gt;</span>
  <span class="c-ac">&lt;services&gt;</span>
    <span class="c-ac">&lt;service</span> name=<span class="c-ok">"db"</span> type=<span class="c-ok">"postgresql:16"</span><span class="c-ac">/&gt;</span>
  <span class="c-ac">&lt;/services&gt;</span>
  <span class="c-ac">&lt;routes&gt;</span>
    <span class="c-ac">&lt;route</span> pattern=<span class="c-ok">"https://{default}/"</span> upstream=<span class="c-ok">"storefront:http"</span><span class="c-ac">/&gt;</span>
  <span class="c-ac">&lt;/routes&gt;</span>
<span class="c-ac">&lt;/project&gt;</span></pre>
            </div>
        </div>
    </section>

    <!-- HOW IT WORKS -->
    <section class="lp-section" id="how" style="padding-top:1rem">
        <div class="lp-section__head">
            <h2>From zero to deployed in six steps</h2>
            <p>No YAML archaeology, no Kubernetes clusters to babysit. Set up your project, connect your repo and ship.</p>
        </div>
        <div class="lp-steps">
            <div class="lp-step">
                <div class="lp-step__n">1</div>
                <h3>Create a project</h3>
                <p>Spin up a new project inside your team — the home for its environments and settings.</p>
            </div>
            <div class="lp-step">
                <div class="lp-step__n">2</div>
                <h3>Add a server</h3>
                <p>Provision an application, database or cache server in the region closest to your users.</p>
            </div>
            <div class="lp-step">
                <div class="lp-step__n">3</div>
                <h3>Add a domain</h3>
                <p>Connect a custom domain and map it to a routing placeholder — TLS is issued automatically.</p>
            </div>
            <div class="lp-step">
                <div class="lp-step__n">4</div>
                <h3>Create a configuration</h3>
                <p>Describe applications, services, mounts and hooks in <span class="mono">tragwerk.xml</span>, versioned with your code.</p>
            </div>
            <div class="lp-step">
                <div class="lp-step__n">5</div>
                <h3>Connect your repository</h3>
                <p>Link a Git repository so every push triggers a build and deploy automatically.</p>
            </div>
            <div class="lp-step">
                <div class="lp-step__n">6</div>
                <h3>Push code</h3>
                <p>Push a branch and Tragwerk builds an image and deploys to an isolated environment with logs and metrics.</p>
            </div>
        </div>
    </section>

    <!-- CTA BAND -->
    <div class="lp-cta-band">
        <h2>Deploy your first app today</h2>
        <p>Start on the free Hobby plan — no credit card required. Upgrade when your team grows.</p>
        <div class="lp-cta-row">
            <a href="<?= $this->url('register') ?>" class="btn btn-light"><i class="bi bi-rocket-takeoff"></i> Start free</a>
            <a href="#" class="btn btn-glass">Compare plans</a>
        </div>
    </div>

</div>

<footer class="tw-footer">
    <a href="<?= $this->url('imprint') ?>">Imprint</a>
    <a href="<?= $this->url('privacy-policy') ?>">Privacy Policy</a>
    <a href="<?= $this->url('terms-and-conditions') ?>">Terms &amp; Conditions</a>
</footer>
