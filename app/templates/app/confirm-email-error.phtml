<?php $this->layout('layout::auth', ['title' => 'Confirmation Failed · Tragwerk']) ?>

<div class="auth-card">
    <div class="auth-card__logo">
        <?= $this->fetch('partial::logo') ?>
    </div>
    <div class="auth-head">
        <h1><?= $this->e($this->t('Confirmation failed')) ?></h1>
        <p><?= $this->e($this->t('This confirmation link is invalid or has expired. Please register again.')) ?></p>
    </div>

    <?= $this->fetch('partial::link', [
        'href'  => $this->url('register'),
        'label' => $this->e($this->t('Back to registration')),
    ]) ?>
</div>
