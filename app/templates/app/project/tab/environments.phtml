<?php
/**
 * @var \Tragwerk\Domain\Entity\Project $project
 */

$listUrl = $this->url('project.environment.branch-list', ['id' => $project->id->toString()]);
?>

<input type="hidden" id="tw-csrf-token" name="csrf_token" value="<?= $this->getCsrfToken() ?>">
<input type="hidden" id="tw-active-branch" value="">

<div class="row g-4">
    <div class="col-12 col-lg-4">
        <div id="tw-branch-list"
             hx-get="<?= $this->e($listUrl) ?>"
             hx-trigger="load"
             hx-swap="outerHTML">
        </div>
    </div>

    <div class="col-12 col-lg-8">
        <div id="environment-activities" class="text-body-secondary small"></div>
    </div>
</div>
