<?php
/** @var \Tragwerk\Domain\Entity\Server $server */
/** @var \Tragwerk\Domain\Entity\SetupJob $job */
$this->layout('layout::default', ['title' => $this->t('Server Setup')])
?>

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="mb-0"><?= $this->t('Setup: {name}', ['name' => $this->e($server->name)]) ?></h1>
    <?= $this->fetch('partial::link', [
        'label'   => $this->t('Back to server'),
        'href'    => $this->url('server.show', ['id' => $server->id->toString()]) . '#setup',
        'classes' => 'btn btn-secondary btn-sm',
    ]) ?>
</div>

<div class="tw-card">
    <?= $this->fetch('page::server/_log_widget', ['server' => $server, 'job' => $job]) ?>
</div>

<script src="/js/setup-log.js"></script>
