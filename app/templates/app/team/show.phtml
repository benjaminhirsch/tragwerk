<?php
/** @var \Tragwerk\Domain\Entity\Team $team */
$this->layout('layout::default', ['title' => $team->name]);
?>

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <nav aria-label="breadcrumb" class="mb-1">
            <ol class="breadcrumb mb-0">
                <li class="breadcrumb-item">
                    <?= $this->fetch('partial::link', ['href' => $this->url('team'), 'label' => _('Teams')]) ?>
                </li>
                <li class="breadcrumb-item active"><?= $this->e($team->name) ?></li>
            </ol>
        </nav>
        <h1 class="mb-0"><?= $this->e($team->name) ?></h1>
    </div>
    <div class="d-flex gap-2">
        <?= $this->fetch('partial::link', [
            'label'   => _('Edit'),
            'href'    => $this->url('team.edit', ['id' => $team->id->toString()]),
            'icon'    => 'bi bi-pencil',
            'classes' => 'btn btn-outline-secondary btn-sm',
        ]) ?>
    </div>
</div>

<ul class="nav nav-tabs" id="teamDetailTabs" role="tablist">
    <li class="nav-item" role="presentation">
        <?= $this->fetch('partial::input/button', [
            'classes'      => 'nav-link active',
            'type'         => 'button',
            'role'         => 'tab',
            'id'           => 'tab-overview-btn',
            'dataBsToggle' => 'tab',
            'dataBsTarget' => '#tab-overview',
            'icon'         => 'bi bi-info-circle me-1',
            'label'        => _('Overview'),
        ]) ?>
    </li>
    <li class="nav-item" role="presentation">
        <?= $this->fetch('partial::input/button', [
            'classes'      => 'nav-link',
            'type'         => 'button',
            'role'         => 'tab',
            'id'           => 'tab-members-btn',
            'dataBsToggle' => 'tab',
            'dataBsTarget' => '#tab-members',
            'hxGet'        => $this->url('team.show.tab', ['id' => $team->id->toString(), 'tab' => 'members']),
            'hxTarget'     => '#tab-members',
            'hxSwap'       => 'innerHTML',
            'hxTrigger'    => 'shown.bs.tab once',
            'icon'         => 'bi bi-people me-1',
            'label'        => _('Members'),
        ]) ?>
    </li>
</ul>

<div class="tab-content tw-card tw-card-tabs">
    <div class="tab-pane fade show active" id="tab-overview" role="tabpanel"
         hx-get="<?= $this->url('team.show.tab', ['id' => $team->id->toString(), 'tab' => 'overview']) ?>"
         hx-trigger="load"
         hx-swap="innerHTML">
    </div>

    <div class="tab-pane fade" id="tab-members" role="tabpanel"></div>
</div>

<?php $this->start('javascript') ?>
<script src="/js/detail-tabs.js"></script>
<?php $this->stop() ?>
