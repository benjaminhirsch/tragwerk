<?php
/** @var \Tragwerk\Domain\Entity\Credential $credential */
$this->layout('layout::default', ['title' => $credential->name]);
?>

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <nav aria-label="breadcrumb" class="mb-1">
            <ol class="breadcrumb mb-0">
                <li class="breadcrumb-item">
                    <?= $this->fetch('partial::link', ['href' => $this->url('credential'), 'label' => $this->t('Credentials')]) ?>
                </li>
                <li class="breadcrumb-item active"><?= $this->e($credential->name) ?></li>
            </ol>
        </nav>
        <h1 class="mb-0"><?= $this->e($credential->name) ?></h1>
    </div>
    <div class="d-flex gap-2">
        <?= $this->fetch('partial::link', [
            'label'   => $this->t('Edit'),
            'href'    => $this->url('credential.edit', ['id' => $credential->id->toString()]),
            'icon'    => 'bi bi-pencil',
            'classes' => 'btn btn-outline-secondary btn-sm',
        ]) ?>
    </div>
</div>

<ul class="nav nav-tabs" id="credentialDetailTabs" role="tablist">
    <li class="nav-item" role="presentation">
        <?= $this->fetch('partial::input/button', [
            'classes'      => 'nav-link active',
            'type'         => 'button',
            'role'         => 'tab',
            'id'           => 'tab-overview-btn',
            'dataBsToggle' => 'tab',
            'dataBsTarget' => '#tab-overview',
            'icon'         => 'bi bi-info-circle me-1',
            'label'        => $this->t('Overview'),
        ]) ?>
    </li>
    <li class="nav-item" role="presentation">
        <?= $this->fetch('partial::input/button', [
            'classes'      => 'nav-link',
            'type'         => 'button',
            'role'         => 'tab',
            'id'           => 'tab-servers-btn',
            'dataBsToggle' => 'tab',
            'dataBsTarget' => '#tab-servers',
            'hxGet'        => $this->url('credential.show.tab', ['id' => $credential->id->toString(), 'tab' => 'servers']),
            'hxTarget'     => '#tab-servers',
            'hxSwap'       => 'innerHTML',
            'hxTrigger'    => 'shown.bs.tab once',
            'icon'         => 'bi bi-hdd-stack me-1',
            'label'        => $this->t('Servers'),
        ]) ?>
    </li>
</ul>

<div class="tab-content tw-card tw-card-tabs">
    <div class="tab-pane fade show active" id="tab-overview" role="tabpanel"
         hx-get="<?= $this->url('credential.show.tab', ['id' => $credential->id->toString(), 'tab' => 'overview']) ?>"
         hx-trigger="load"
         hx-swap="innerHTML">
    </div>

    <div class="tab-pane fade" id="tab-servers" role="tabpanel"></div>
</div>

<?php $this->start('javascript') ?>
<script src="/js/detail-tabs.js"></script>
<?php $this->stop() ?>
