<?php
/** @var \Tragwerk\Domain\Entity\Credential $credential */
?>
<table class="table table-borderless mb-0">
    <tbody>
        <tr>
            <th class="w-25 text-body-secondary fw-normal"><?= $this->t('Name') ?></th>
            <td><?= $this->e($credential->name) ?></td>
        </tr>
        <tr>
            <th class="text-body-secondary fw-normal"><?= $this->t('Username') ?></th>
            <td><code><?= $this->e($credential->username) ?></code></td>
        </tr>
        <tr>
            <th class="text-body-secondary fw-normal"><?= $this->t('SSH Key') ?></th>
            <td>
                <?php if ($credential->privateKey !== null): ?>
                    <i class="bi bi-check-circle-fill text-success me-1"></i><?= $this->t('Configured') ?>
                <?php else: ?>
                    <i class="bi bi-x-circle text-secondary me-1"></i><?= $this->t('Not configured') ?>
                <?php endif ?>
            </td>
        </tr>
        <tr>
            <th class="text-body-secondary fw-normal"><?= $this->t('Created') ?></th>
            <td><?= $this->e($credential->createdAt->format('Y-m-d H:i')) ?></td>
        </tr>
    </tbody>
</table>
